apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: nextcloud-test
  namespace: flux-system
spec:
  interval: 10m
  path: clusters/main/kubernetes/tests/nextcloud-test/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: cluster